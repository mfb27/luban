分析仓库中所有未提交的更改，生成中文提交信息，并推送到远程仓库。

步骤：
1. 运行 `git status` 查看未提交的更改
2. 运行 `git diff` 理解更改内容
3. 运行 `git log --oneline -5` 了解提交信息风格
4. 用中文起草提交信息，格式：`feat: xxx` 或 `fix: xxx` 等
5. 添加相关文件并提交
6. 推送到远程仓库

如果没有更改需要提交，不要推送。提交信息必须使用中文。